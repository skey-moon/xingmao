<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xingmao.mapper.DeliveryPersonMapper">
    <update id="incrementOrderCount">
        UPDATE delivery_person
        SET order_count = order_count + 1
        WHERE id = #{id}
    </update>

    <update id="decrementOrderCount">
        UPDATE delivery_person
        SET order_count = GREATEST(order_count - 1, 0)
        WHERE id = #{id}
    </update>

    <select id="selectAvailablePersonWithLock" resultType="com.xingmao.entity.DeliveryPerson">
        SELECT * FROM delivery_person
        WHERE deleted = 0 AND status = 0
        ORDER BY order_count ASC
        LIMIT 1
        FOR UPDATE
    </select>

    <select id="selectLeastBusyWithLock" resultType="com.xingmao.entity.DeliveryPerson">
        SELECT * FROM delivery_person
        WHERE deleted = 0
        ORDER BY order_count ASC
        LIMIT 1
        FOR UPDATE
    </select>
</mapper>